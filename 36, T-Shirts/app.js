function make_shirt(size, printMessage) {
    console.log("you selected ".concat(size, " size shirt ").concat(printMessage, " prints on shirt "));
}
// for call function
make_shirt("medium", "code with Rizwanali2202");
// if we want again call function then
make_shirt("small", "Rizwan ali");
